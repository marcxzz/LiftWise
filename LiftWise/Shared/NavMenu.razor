<nav class="navbar navbar-expand-lg navbar-dark fixed-top" style="background-color: rgba(0, 0, 0, .9);">
    <div class="container m-0 p-2">
        <a class="navbar-brand" href="#">LiftWise</a>
        <button class="navbar-toggler" type="button" @onclick="ToggleNavMenu" aria-controls="navbarResponsive"
                aria-expanded="@(!collapseNavMenu)" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse @(collapseNavMenu ? "" : "show")" id="navbarResponsive">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <NavLink class="fw-bold m-1" href="">
                        Profilo <span class="oi oi-person"></span>
                    </NavLink>
                </li>
                <li class="nav-item">
                    <NavLink class="fw-bold m-1" href="">
                        Home <span class="oi oi-home" aria-hidden="true"></span>
                    </NavLink>
                </li>
            </ul>
        </div>
    </div>
</nav>

@code {
    private bool collapseNavMenu = true;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
